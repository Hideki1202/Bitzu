<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo -->
    <solid android:color="@color/weakPurple" />

    <!-- Borda -->
    <stroke
        android:width="2dp"
        android:color="@color/purple" /> <!-- Aqui muda a cor da borda -->

    <!-- Raio da borda (opcional) -->
    <corners android:radius="8dp" />
</shape>